<div id="searchIndex" class="inner">
    
    <div id="results">
        <?php 
            foreach($this->results as $product) { 
                echo $this->partial('product_partial.phtml', array('product'=>$product));
            }

        ?>

    </div>

</div>
